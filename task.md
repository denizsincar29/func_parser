# Task
On completing each task, please mark it as done, because sometimes you stop the session with a rate limit error, and you can just continue the session later without losing track of what you have done and what is left to do. Also, if you have any questions or need clarification on any task, feel free to ask. When I write a comment with something new to make in the code, add it to the task list.
1. ( ) Make the python code runable and managed by UV package manager, and make sure everything including pytests are working with uv run.
2. ( ) help and exit commands should be togglable in the command parser initializer, not to create a @parser.command for help and exit. That can be also implemented in rust version.
3. ( ) For the one letter aliases, we can have "+" sign before the alias letter, like "+help" will be /help and /h, and "e+xit" is /exit and /x, plus you can add more aliases like "+quit", so the + sign will be valid for the main command + aliases. It must raise an error if there are duplicate commands/aliases, and i guess compile error in rust version, the same logic with +commands for single letter aliases can be applied in rust.
4. ( ) Command's help can be in the function's doc string, not always explisitly in help= argument. In rust, we can have a doc comment before the attribute, like in clap. Idk how to implement each arg help via docstring's args syntax, search the web for kinda doc parsers, but in rust the each arg can have a doc comment before each arg attribute, like in clap.
5. ( ) add support for a few types in same argument, because int can be added to float and get a float result. Even we can add some kinda trait system, like addable / subtractable etc in both python and rust, but IDK how to make this very efficiently.
6. ( ) CTX should have kinda expirable values, a value can have a ttl or a datetime expiration. Both in python and rust.
7. ( ) If command parser is in cli mode, you can have kinda parser.run() method that will automatically read stdin and execute commands, running it's default exit command on ctrl+c or if it's off, raise system exit or whatever. And parser.loop() is a generator that yields command results, so you can have more control over the loop, like breaking it on some condition or something. Both in python and rust.
8. ( ) Adapt every test and example to use all the new features, and make sure to have good coverage for all the features, especially the new ones. Both in python and rust.
9. ( ) Write rust examples, for now there are just basic.rs, but we need examples for all the features, and also some complex examples that use multiple features together.
10. ( ) write a good gitignore and remove everything that should be ignored. Remember, python version must be on uv package manager.